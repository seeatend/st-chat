# Meteor.publish 'paymentSubscriptions', (orgId) -> #XXX
# 	unless this.userId
# 		return this.ready()
# 	Subscriptions.find()

# Meteor.publish 'paymentHistory', (orgId) -> #XXX
# 	unless this.userId
# 		return this.ready()
# 	PaymentHistory.find()
